{
  "name": "str-parser",
  "version": "0.0.6",
  "description": "String parser for Node.js.",
  "main": "index.js",
  "engines": {
    "node": ">=0.12.0"
  },
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha --reporter spec",
    "coveralls": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | coveralls && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Lellansin/str-parser.git"
  },
  "author": "lellansin",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Lellansin/str-parser/issues"
  },
  "homepage": "https://github.com/Lellansin/str-parser#readme",
  "devDependencies": {
    "coveralls": "^2.11.9",
    "istanbul": "^0.4.3",
    "mocha": "^2.4.5",
    "should": "^8.3.1"
  }
}
